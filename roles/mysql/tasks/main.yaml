- name : install mysql
  ansible.builtin.package:
    name : mysql-server
    state : present

- name : start and enable mysql
  ansible.builtin.service:
    name : mysqld
    state : started
    enabled : yes

- name : setup ropt password
  ansible.builtin.command: mysql_secure_installation --set-root-pass {{ mysql_root_password }}
  